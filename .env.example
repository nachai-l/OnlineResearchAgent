# Copy to .env (gitignored) and fill in real values.
# The *_env pointers in configs/credentials.yaml map to these names.

SERPAPI_API_KEY=
GOOGLE_CSE_API_KEY=
GOOGLE_CSE_CX=

# LLM credentials — fill in only the backend you use (parameters.yaml -> llm.backend).
GEMINI_API_KEY=
LITELLM_API_KEY=

# A2A_PUBLIC_URL=http://host.docker.internal:8000
