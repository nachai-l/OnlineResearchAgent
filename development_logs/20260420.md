# Development log — 2026-04-20

## Goal for the day

Close out the A2A Online Research Agent against the v2 plan (TDD + JSONL
cache + structured logging + A2A wrapping). Starting point: 10 core
modules done (hashing, logging, config, cache, prompts, llm client,
search, scraping, validation, summarization), with summarization
verification still pending from the previous session.

## What shipped

### Verified: `core/summarization.py`

- Ran `pytest tests/test_summarization.py` → 4/4 green.
- Ran full suite → 82/82 green. No regressions.

### New TDD cycle: `core/selection.py`

- The plan listed `prompts/result_selection.yaml` but no corresponding
  module. Added `ResultSelector` as a sibling of `PageValidator` /
  `Summarizer` rather than inlining the LLM call in the pipeline —
  keeps the pipeline focused on orchestration.
- Contract: `async select(query, results, *, top_k) -> list[SearchResult]`.
- Garbage / malformed LLM response falls back to the first `top_k`
  candidates so the pipeline still makes progress.
- Out-of-range indices and duplicates are filtered; order preserved.
- Empty input returns `[]` without invoking the LLM.
- Tests: 6/6 (`test_selection.py`).

### New TDD cycle: `core/research_pipeline.py`

- Orchestrates: search → select → scrape → validate → summarize.
- Every subcomponent injected — the pipeline itself knows nothing about
  HTTP or the Gemini SDK. That's what lets the end-to-end test run
  offline with `respx` + a stub LLM callable.
- `ResearchResult` dataclass carries summary markdown, sources, the
  full list of `PageJudgement`s, plus search/scrape/kept counts for
  downstream telemetry.
- **Critical invariant proven**: running the same query twice issues
  **0** extra HTTP calls + **0** extra LLM calls on the second run.
  All three JSONL caches (search, scrape, llm) serve it end to end.
- Empty search results short-circuit to a graceful summary with no
  LLM call whatsoever.
- Tests: 4/4 (`test_research_pipeline.py`).

### New: A2A wrapping — `agent/skills.py` + `agent/agent_card.py` + `agent/server.py`

- Installed `a2a-sdk[http-server]` (0.3.26) + `sse-starlette`.
  Updated `requirements.txt`.
- `ResearchAgentExecutor(AgentExecutor)`:
  - Reads the query from `context.message.parts` (handles the pydantic
    `RootModel`-wrapped `Part` by walking `.root.text`).
  - Event sequence: `TaskStatusUpdateEvent(working)` →
    `TaskArtifactUpdateEvent` (summary + sources as two `TextPart`s) →
    `TaskStatusUpdateEvent(completed, final=True)`.
  - Missing / whitespace-only query short-circuits to
    `state=failed, final=True` with zero pipeline invocations.
  - Any pipeline exception surfaces as `state=failed, final=True`
    (logged with `exception()`).
  - `cancel()` emits `state=canceled, final=True`.
- `build_agent_card(url)`: pure function returning the
  `AgentCard` with capabilities `{streaming: True}`, one skill
  (`web_research`) with tags, examples, and text-plain input /
  text-markdown output modes.
- `build_app(executor, url)`: wraps `A2AStarletteApplication` over
  `DefaultRequestHandler` + `InMemoryTaskStore`. Starlette
  `TestClient` fetches `/.well-known/agent-card.json` and asserts
  the manifest without touching the network.
- Tests: 5 skill + 3 card + 2 server = 10/10.

### New: `functions/agent/wiring.py`

- Single `build_pipeline(settings)` function used by both
  `run_local.py` and `run_server.py`. Keeps the bootstrap DRY
  and ensures both entry points share caches + prompts + settings.
- Loads all three prompt templates from `prompts/*.yaml`.
- Creates `JsonlCache` only when `settings.cache.enabled` is True
  (otherwise passes `None` to each subcomponent — they all tolerate
  it). Covered indirectly through the live smoke; not unit-tested
  because each subcomponent already has its own cache tests.

### New: prompts

- `prompts/result_selection.yaml` — strict JSON schema
  `{"picks": [<int>, ...]}`.
- `prompts/validation.yaml` — strict JSON schema
  `{"relevance": <float>, "trustworthiness": <float>, "reason": "<short>"}`
  with concrete score-band guidance.
- `prompts/summarization.yaml` — inline `[n]` citation rule, no
  trailing "Sources" section (the caller appends it).

### New: scripts

- `scripts/run_local.py` — CLI smoke. Loads settings, configures
  logging, runs the pipeline, prints the markdown summary + numbered
  sources.
- `scripts/run_server.py` — starts uvicorn with the A2A app on
  `settings.server.host:port`. Passes `log_config=None` to uvicorn
  so our `configure_logging` owns the root logger.
- `scripts/clear_cache.py` — truncates all three JSONL files to
  zero length, preserving the directory.

## Final verification

```
pytest --cov=functions --cov-report=term-missing
102 passed in 52.53s
TOTAL  779 stmts  69 miss  91% cover
```

Per-module coverage all ≥ 83%, core + utils modules 91-100%. Well
above the 85% floor the plan called for.

Dry-imported the full wiring against the real YAMLs to confirm no
path / import issue:

```
wiring OK; pipeline type: ResearchPipeline
search top_k: 8
cache dir: .../artifacts/cache/search_cache.jsonl
log path: .../artifacts/logs/agent.log
```

Ran `scripts/clear_cache.py` — exits 0 when cache files are absent
(skipped cleanly rather than erroring).

## Decisions worth remembering

- **Selection as its own module.** Plan listed the prompt but not
  the module. Splitting it out mirrors the `validation.py` /
  `summarization.py` pattern and made the pipeline trivially
  testable as pure orchestration.
- **Event stream stays coarse.** Chose `working → artifact →
  completed` at the A2A layer rather than emitting one status per
  internal stage. Internal stages already log structured lines;
  re-emitting them as A2A events would flood clients with detail
  they can't act on. If a future client wants fine-grained progress
  we can add it as an opt-in extension.
- **Agent Card URL is built once at server-startup.** The card
  reflects the configured `server.host:port`. Dynamic card
  modifiers are an SDK feature but not needed at this scale.
- **Wiring is an integration seam, not new logic.** Intentionally
  not unit-tested — its only job is to pass the right fields to
  already-tested constructors. If a bug lands there it surfaces in
  the live smoke.
- **`sse-starlette` is a hard dep for the server.** Added
  `a2a-sdk[http-server]` + explicit `sse-starlette` to
  `requirements.txt` so a fresh checkout installs everything.

## Addendum: LiteLLM backend + Docker deployment

After the live smoke shook out (key-leak fix in `utils/logging.py`,
zombie-cache auto-heal in `core/scraping.py`, full missing-deps
install), the agent was asked to (a) talk to an internal LiteLLM proxy
instead of Gemini directly, and (b) deploy as a container.

### LiteLLM as a second LLM backend

Already had the hard work done: `llm/client.py` exposes a `CallFn`
Protocol and the production path goes through `default_call_fn`. So
the integration is purely additive.

- **New**: `default_litellm_call_fn(*, base_url, api_key)` in
  `functions/llm/client.py`. POSTs canonical OpenAI chat-completions
  (`{"model", "messages": [system, user], "temperature",
  "max_tokens"}`) to `{base_url}/chat/completions` with
  `Authorization: Bearer ...`, reads `choices[0].message.content`.
  Trailing slashes on `base_url` are tolerated. Empty `choices`
  raises `LlmCallError` instead of returning an empty string so the
  retry envelope can catch it.
- **Timeout**: `httpx` alone wasn't enough — respx mocks skip the
  transport-layer timeout. Wrapped the request in
  `asyncio.wait_for(..., timeout=timeout)` so the contract holds
  against any transport and the timeout test is meaningful.
- **Tests**: 5/5 (`tests/test_llm_litellm.py`) — OpenAI shape +
  Authorization header, trailing-slash base_url, non-2xx → HTTPStatusError,
  empty choices → LlmCallError, timeout propagation.

Backend selection is a config flag, not code:

- `LlmSettings.backend: Literal["gemini", "litellm"]` (default
  `"gemini"`, switched to `"litellm"` in `parameters.yaml`).
- `LlmSettings.litellm_base_url: str | None` — only consulted when
  backend is `"litellm"`.
- `Credentials.litellm_api_key: str | None` — resolved from
  `LITELLM_API_KEY` via the existing env-var-pointer pattern in
  `credentials.yaml`.
- `functions/agent/wiring.py` grew a private `_build_call_fn` that
  picks the right factory and raises a clear error at startup if the
  wrong credentials are missing, rather than lazy-failing on the
  first LLM request. `GeminiClient` is now always constructed with
  `call_fn=<the chosen fn>` and `api_key=None` — the legacy
  "build-SDK-fn-on-demand" path in the client still works but the
  wiring no longer relies on it.

Key design intent: the name `GeminiClient` is now a misnomer (it's
just "the LLM client with cache + retry + JSON parsing") but I didn't
rename it. That's a cosmetic change worth one PR on its own; squeezing
it into this one would have made the diff noisy.

### Docker deployment

Three new files at the repo root. No source touched except
`scripts/run_server.py`.

- **`Dockerfile`** — two-stage build on `python:3.12-slim`.
  - `builder` stage has `build-essential` + `libxml2-dev` +
    `libxslt1-dev` so trafilatura's `lxml` wheel compiles cleanly.
    Deps install into `/opt/venv`; nothing else from the stage
    survives into the runtime image.
  - `runtime` stage pulls in runtime-only `libxml2` + `libxslt1.1`,
    copies the venv + source tree (`functions`, `prompts`, `configs`,
    `scripts`), creates `/app/artifacts/{cache,logs}`, and switches
    to a non-root `app` user. Defaults
    `A2A_HOST=0.0.0.0 A2A_PORT=8000`.
  - `HEALTHCHECK` hits
    `/.well-known/agent-card.json` via `urllib` — no extra dep.
- **`.dockerignore`** — excludes `.env*`, `.venv/`, caches, tests,
  docs, `.git/`. The image is source + deps, nothing else.
- **`docker-compose.yml`** — one service. Reads `.env` via
  `env_file`. Binds host port `${A2A_HOST_PORT:-8000}` →
  container 8000. Bind-mounts `./artifacts/cache` and
  `./artifacts/logs` so JSONL caches and logs survive
  `docker compose down` and are greppable from the host.
  `A2A_PUBLIC_URL` defaults to `http://localhost:8000` and can be
  overridden for reverse-proxy deployments.

`scripts/run_server.py` now reads three optional env-var overrides:

- `A2A_HOST` — bind address (defaults to `parameters.yaml`).
- `A2A_PORT` — bind port.
- `A2A_PUBLIC_URL` — URL advertised in the Agent Card.

That last one matters: inside a container the app binds `0.0.0.0:8000`,
but clients need the externally reachable URL
(`http://localhost:8000` locally, `https://...` behind a proxy).
Without `A2A_PUBLIC_URL` the card would advertise `0.0.0.0:8000` which
is never what a client wants.

### Final verification

```
pytest -q
109 passed in 41.06s
```

All previously green tests still green; 5 new LiteLLM tests added
(102 → 109 total). `parameters.yaml` switched to the LiteLLM backend
so the default `docker compose up --build` path now targets the
internal proxy.

## Addendum: pydantic structure validation for LLM output

Audit exposed an asymmetry — config loading was already strict
(`BaseModel` subclasses wall-to-wall with `Field(ge=..., le=...)`), but
every `generate_json` consumer was hand-rolling dict parsing
(`.get("relevance", 0.0)`, `float(...)`, clamping). A malformed or
drifted response downgraded silently instead of raising, which can
look like "the model always scored 0" for weeks before anyone notices.

### Client refactor

- **One retry budget, three purposes.** Folded JSON parsing and schema
  validation *inside* `_invoke_with_retry` so `llm.retries` now covers
  transport, JSON, and schema failures uniformly. A model that
  occasionally emits truncated JSON or an out-of-range score gets a
  chance to self-correct on the next attempt — exactly the behavior
  you'd want but we didn't have.
- **New `generate_model[TModel: BaseModel]`.** Parses + validates with
  `Model.model_validate_json(_strip_code_fences(text))`. Both
  `ValidationError` and `json.JSONDecodeError` surface as `LlmCallError`
  so the retry envelope catches them with no special-casing.
- **`generate_json` upgraded in place.** Still returns `dict[str, Any]`,
  but parse failures now retry. Existing callers that already handle
  `LlmCallError` on exhaustion are unaffected; callers that were
  relying on the old "one bad JSON raises immediately" behavior are
  unaffected *if* they leave `retries > 0` (retries + eventual failure
  still end in `LlmCallError`).
- **Cache self-heal on schema drift.** Cache hits are now run through
  the post-processor. If the cached response no longer satisfies the
  current `response_model` (e.g. the caller upgraded the schema), the
  hit is logged and treated as a miss, a fresh response is fetched,
  and last-write-wins shadows the stale record. Same pattern as the
  zombie-scrape fix.
- **Single private helper.** `_generate_parsed(post_process, ...)` now
  backs `generate` (identity), `generate_json` (JSON object), and
  `generate_model[T]` (pydantic). No duplicated cache-check / retry
  logic across the three public entry points.

### Consumer cleanup

Two consumers got noticeably shorter:

- `core/selection.py`: added `_SelectionResponse(BaseModel)` with
  `picks: list[int]`. Dropped the `isinstance(..., Iterable)` check
  and the per-item `int()` coercion — pydantic rejects non-list /
  non-int picks before we ever see them. `_materialize` is now just
  range + dedupe. Only remaining fallback is "no indices survived
  range filtering" — a model that answered the wrong question is
  retried by the client before reaching this layer.
- `core/validation.py`: added `_ValidationResponse(BaseModel)` with
  `relevance: float = Field(ge=0, le=1)` and
  `trustworthiness: float = Field(ge=0, le=1)`. Dropped the manual
  `float()` + clamp — out-of-range scores now fail validation (and
  retry) instead of being silently clamped, which matters because
  silent clamping hides a prompt regression.

### Why no new config knob

Considered `llm.validation_retries` as a separate counter but decided
one global `llm.retries` is both simpler and correct: the failure
modes (transport hiccup, malformed JSON, off-spec JSON) have the same
remediation — retry once, maybe twice, then give up and bubble.
Splitting retries across two knobs would mean each call carried two
budgets, and tuning becomes a coupled optimization.

### Tests

Added 7 new tests in `test_llm_client.py`:

- `test_generate_json_retries_on_parse_failure` — 2 bad JSON then
  1 good → returns the good parse after 3 calls.
- `test_generate_model_validates_and_returns_instance`
- `test_generate_model_strips_code_fences`
- `test_generate_model_retries_on_schema_violation` — one
  `Field(ge=0)` violation, one missing-required-field, one valid.
- `test_generate_model_exhausted_raises` — all 3 fail → `LlmCallError`.
- `test_generate_model_caches_only_valid_responses` — bad attempts
  don't pollute the cache; the first valid response is what gets
  stored.
- `test_cache_hit_failing_current_schema_is_refetched` — the
  self-heal / schema-drift case.

Also tightened `test_generate_json_invalid_raises` to `retries=0`; the
original single-bad-response case now requires that explicit setting
since retries are now shared with parse failures.

Final: **116 passed in 58.47s** (109 → 116; +7 new tests, 0 regressions).

## Addendum: catalog registration

User confirmed the downstream UI ("Register Agent" form) takes a
**base URL** and fetches `/.well-known/agent-card.json` automatically.
Two things shook out of that:

- The Agent Card's `url` field (built at startup in
  `scripts/run_server.py` from `A2A_PUBLIC_URL`) is what the catalog
  will hand to clients for subsequent `message/send` calls. It must
  match what you registered, not the in-container bind address. The
  env-var override was already in place — now the README also spells
  out the three common host/container permutations and the
  `host.docker.internal` case.
- No code change needed for catalog compatibility: the SDK already
  serves the card at the canonical path, and `capabilities.streaming`
  is already advertised. `capabilities.jsonrpc` is implicit (the SDK
  owns that).

README gained a "Register with the agent catalog" section with a
host/container matrix + a concrete `A2A_PUBLIC_URL` snippet for
`docker-compose.yml`. No implementation drift.

## Pointers for tomorrow

1. **Live smoke against LiteLLM** — populate `LITELLM_API_KEY` in
   `.env`, run `docker compose up --build`, hit with
   `scripts/client_smoke.py`. Verify cache hits work across container
   restarts (bind-mounted `artifacts/cache` should persist).
2. **Rename `GeminiClient` → `LlmClient`** — cosmetic, one PR.
   Leaving this to a dedicated change keeps diffs legible.
3. **Catalogue port** — add a thin `async def web_research(query,
   **kwargs) -> str` shim that wraps `ResearchPipeline.run` and
   returns `result.summary_markdown`, plug into the sibling
   catalogue's `executor_dict`.
